spring:
  application:
    name: "questionbank-service"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/quesbnkdb
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect

#Temp commented--
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
  info:
    env:
      enabled: true

endpoints:
  shutdown:
    enabled: true

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8060/eureka/

info:
  app:
    name: "questionbank-service"
    description: "QuestionBank Service for Assessment Platform"

#Temp commented--

server:
  port: 8081

#  servlet:
#    context-path: /questionbank-service

  #  instance:
  #    preferIpAddress: true